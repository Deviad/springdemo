server:
  servlet:
#    context-path: /api
     context-path: /
---
spring:
  profiles: development
  jpa:
    hibernate:
#      ddl-auto: update
      ddl-auto: update
    database: mysql
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
  datasource:
    url: jdbc:mysql://localhost/${DB_NAME}?useLegacyDatetimeCode=false&serverTimezone=UTC&useSSL=false
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
security:
  oauth2:
    client:
      client-id: fooClientIdPassword
      client-secret: secret
      access-token-uri: http://localhost:8901/oauth/token
    resource:
      user-info-uri: http://localhost:8901/oauth/token
server:
  port: ${SERVER_PORT}

---
spring:
  profiles: production
  jpa:
    hibernate:
      ddl-auto: none
    database: mysql
server:
  port: ${SERVER_PORT}